@model VerifyForgotPasswordOtpRequest
@{
    Layout = "_Layout";
    ViewData["Title"] = "OTP Verification";
}

@section Styles {
    <link rel="stylesheet" href="~/css/VerifyOTP.css" asp-append-version="true" />
}

<section class="otp-page" aria-labelledby="otp-title">
    <div class="otp-card">
        <div class="otp-icon">OTP</div>
        <h1 id="otp-title">Verify OTP</h1>
        <p>Enter the 6-digit code sent to your registered email address.</p>

        @if (TempData["SuccessMessage"] is string successMessage)
        {
            <div class="alert alert-success" role="status">@successMessage</div>
        }
        <div asp-validation-summary="ModelOnly" class="alert alert-warning" role="alert"></div>

        <form method="post" asp-controller="Account" asp-action="VerifyOTP">
            @Html.AntiForgeryToken()
            <input asp-for="Email" type="hidden" />
            <label class="form-label" asp-for="OtpCode">6-digit OTP Code</label>
            <input class="form-control form-control-lg otp-input" asp-for="OtpCode" type="text" inputmode="numeric" maxlength="6" placeholder="000000" required />
            <span class="text-danger" asp-validation-for="OtpCode"></span>
            <div class="d-flex gap-2 mt-4">
                <button class="btn btn-primary flex-fill" type="submit">Verify OTP</button>
                <a class="btn btn-outline-secondary flex-fill" asp-controller="Account" asp-action="ForgotPassword">Resend OTP</a>
            </div>
        </form>

        <a class="otp-return" asp-controller="Account" asp-action="ForgotPassword">Back to ForgotPassword</a>
    </div>
</section>
