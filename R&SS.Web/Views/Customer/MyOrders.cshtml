@model IReadOnlyCollection<OrderResponse>
@{
    Layout = "_Layout";
    ViewData["Title"] = "Đơn hàng của tôi";
    ViewData["ActiveRole"] = "Customer";
    ViewData["RoleDisplay"] = "Khách hàng";
}

@section Styles {
    <link rel="stylesheet" href="~/css/MyOrders.css" asp-append-version="true" />
}

<section class="orders-page">
    <div class="orders-header">
        <h1>Đơn hàng của tôi</h1>
        <form method="get" asp-controller="Customer" asp-action="MyOrders">
            <input class="form-control" name="keyword" type="search" value="@ViewData["Keyword"]" placeholder="Tìm theo mã đơn hàng..." />
        </form>
    </div>
    <div class="orders-panel table-responsive">
        <table class="table align-middle">
            <thead><tr><th>Mã đơn hàng</th><th>Trạng thái</th><th>Tổng tiền</th><th>Thao tác</th></tr></thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr><td colspan="4">Không tìm thấy đơn hàng.</td></tr>
                }
                else
                {
                    @foreach (var order in Model)
                    {
                        <tr>
                            <td>@order.SalesOrderCode</td>
                            <td><span class="badge text-bg-info">@order.Status</span></td>
                            <td>@order.TotalAmount.ToString("C")</td>
                            <td><a class="btn btn-sm btn-outline-primary" asp-controller="Customer" asp-action="OrderDetails" asp-route-id="@order.SalesOrderId">Xem chi tiết</a></td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</section>
